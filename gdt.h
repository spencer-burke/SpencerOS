// Include the headers for standard numbers as they are needed to describe the table
#include <stdint.h>

// Define and pack the gdt table entry
typedef struct gdt
{
} __attribute__ ((packed)) gdt_table_entry;

typedef struct gdt
{
} __attribute__ ((packed)) gdt_table;
